import { NextResponse } from "next/server";import { apiErrorResponse,parseJsonBody } from "@/lib/api/errors";import { rpc,userScopedSupabase } from "@/lib/api/auth";import { shareOpenRequestSchema } from "@/lib/api/schemas";
export async function POST(request:Request){try{const body=shareOpenRequestSchema.parse(await parseJsonBody(request));await rpc(userScopedSupabase(request),"record_share_open",{p_share_token:body.shareToken,p_challenge_slug:body.challengeSlug});return NextResponse.json({ok:true},{headers:{"Cache-Control":"no-store"}});}catch(error){return apiErrorResponse(error);}}
