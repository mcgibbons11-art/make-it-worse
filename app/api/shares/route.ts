import { NextResponse } from "next/server";import { apiErrorResponse,idempotencyKey,parseJsonBody } from "@/lib/api/errors";import { rpc,userScopedSupabase } from "@/lib/api/auth";import { createShareRequestSchema } from "@/lib/api/schemas";
export async function POST(request:Request){try{const body=createShareRequestSchema.parse(await parseJsonBody(request));const data=await rpc(userScopedSupabase(request),"create_share",{p_challenge_slug:body.challengeSlug,p_channel:body.channel,p_idempotency_key:idempotencyKey(request)});return NextResponse.json(data,{headers:{"Cache-Control":"no-store"}});}catch(error){return apiErrorResponse(error);}}
