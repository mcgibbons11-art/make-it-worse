import type { Metadata } from "next";import HomePageClient from "@/components/home/HomePageClient";
export const metadata:Metadata={title:"MAKE IT WORSE — Beat it. Ruin it. Send it.",description:"Beat a toy-like 3D obstacle course, add one ridiculous trap, and send the worse version to a friend."};
export default function Home(){return <HomePageClient/>;}
