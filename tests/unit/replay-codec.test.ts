import { describe,expect,it } from "vitest";import { decodeGhostTrace,encodeGhostTrace,interpolateGhost } from "@/lib/game/replay-codec";
describe("replay codec",()=>{it("round trips within 1.5cm",()=>{const sample={x:1.234,y:2.345,z:3.456,yaw:.789,flags:3};const decoded=decodeGhostTrace(encodeGhostTrace([sample]))[0]!;expect(Math.abs(decoded.x-sample.x)).toBeLessThan(.015);expect(Math.abs(decoded.y-sample.y)).toBeLessThan(.015);expect(Math.abs(decoded.z-sample.z)).toBeLessThan(.015);});it("rejects oversized or malformed traces",()=>{expect(()=>encodeGhostTrace(Array.from({length:901},()=>({x:0,y:0,z:0,yaw:0,flags:0})))).toThrow();expect(()=>decodeGhostTrace({version:1,hz:15,durationMs:1,frames:[[0,0]]})).toThrow();});it("interpolates endpoints",()=>{const a={x:0,y:0,z:0,yaw:0,flags:1};const b={x:2,y:4,z:6,yaw:1,flags:2};expect(interpolateGhost(a,b,0)).toEqual(a);expect(interpolateGhost(a,b,1)).toEqual(b);});});
